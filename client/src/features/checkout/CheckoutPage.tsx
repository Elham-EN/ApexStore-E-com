import { Typography } from "@mui/material";
import React from "react";

export default function CheckoutPage(): React.ReactElement {
  return (
    <Typography>Only logged in user should be able to see this</Typography>
  );
}
